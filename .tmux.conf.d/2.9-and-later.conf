# Escキーの応答遅延を短縮 (デフォルト500ms -> 10ms)
set-option -sg escape-time 10

# エスケープシーケンスのパススルー許可
set-option -g allow-passthrough on

# 拡張キー入力（CSI u）の対応
set-option -g extended-keys on

# ステータスバーの表示
set-option -g status-style fg=colour0,bg=colour153

# ステータスバーの左側にホスト名を表示
set-option -g status-left " #h "
set-option -g status-left-length 40
set-option -g status-left-style fg=colour196,bg=colour255

# ステータスバーの右側に日次を表示
set-option -g status-right " [%m/%d %H:%M] "
set-option -g status-right-length 16
set-option -g status-right-style fg=colour21,bg=colour255

# ウィンドウ一覧部における現在非アクティブなウィンドウの項目の表示
set-window-option -g window-status-style fg=colour0,bg=default

# ウィンドウ一覧部における現在アクティブなウィンドウの項目の表示
set-window-option -g window-status-current-style fg=colour255,bg=colour244

# Claude Codeが入力待ち(@claude_waiting=1)のウィンドウにマーカーを表示
# (Claude Code側のhookが `tmux set-window-option -t <pane> @claude_waiting 0|1` で更新する)
set-window-option -g window-status-format "#{?#{==:#{@claude_waiting},1},#[fg=colour196 bold]!!#[default],}#I:#W#F#{?#{==:#{@claude_waiting},1},#[fg=colour196 bold]!!#[default],}"
set-window-option -g window-status-current-format "#{?#{==:#{@claude_waiting},1},#[fg=colour196 bold]!!#[default],}#I:#W#F#{?#{==:#{@claude_waiting},1},#[fg=colour196 bold]!!#[default],}"

# pane 同士の境界線
set-option -g pane-active-border-style fg=colour15

# 新しいウィンドウ/ペイン分割 (元のペインと同じディレクトリで開く)
bind C-c new-window -c '#{pane_current_path}'            # 新しいウィンドウ
bind 2 split-window -v -c '#{pane_current_path}'         # 上下に分割する
bind 3 split-window -h -c '#{pane_current_path}'         # 左右に分割する

# message text
set-option -g message-style fg=colour196,bg=colour15

# # ウィンドウ名の自動変更を有効化
# set-option -g allow-rename on
# set-window-option -g automatic-rename on

